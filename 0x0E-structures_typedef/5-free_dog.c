#include "dog.h"
#include <stdlib>

/**
  * free_dog - This frees the dog
  * @d: Dog pointer
  */
void free_dog(dog_t *d)
{
	d->name = malloc(...);
	d->owner = malloc(..);
	dog_t *p = malloc(..);

	free(d->name)
	free(d->owner)
	free(d);
	d = null
}
