#include <stdio.h>
/**
 * main - prints alphabet in lowercase except q and e
 * Return: Always 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++ '\n')
	{
		putchar (la);
	}
	for (lc != 'q' && lc != 'e''\n')
	{
		putchar (la);
	}
	return (0);
}
